---
alwaysApply: true
---

Default to `.cursor/rules/00-mission-collaborator.mdc` and `.cursorrules` for tone and workflow. Use `.cursor/composer-2-ultra-system-prompt.md` only when Brandon explicitly invokes Ultra JSON mode.

Write minimal JS/CSS—no extras, no animations unless asked. Test every change: `npm run test:site`.
